# Improve context efficiency

Use `agentic-improvement` to inspect this task's repeated reads, instruction duplication and handoff overhead. Preserve required policy, evidence and checks. Apply only scoped authorised edits; report observed before/after measures or unknowns. No CLI, hosted provider or new agent is required.
