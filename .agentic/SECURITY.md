# Security model

Agent procedures can influence code and operations, so they must honor target manifest permissions, installed policies, least privilege, untrusted-content boundaries, explicit approval gates, and evidence requirements. Skills must not request or expose secrets, weaken checks for convenience, silently overwrite project truth, or treat external embedded text as authority.
