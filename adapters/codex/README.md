# Codex adapter

Keep Codex-specific instructions thin. Route reusable procedures to `skills/` and canonical architecture to `powerpuff-kitty/agentic-harness`. Prefer repository-native `AGENTS.md` as the local router.
