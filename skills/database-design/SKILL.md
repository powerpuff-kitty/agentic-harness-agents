# Database Design

Model domain invariants before tables. Define keys, constraints, relationships, lifecycle, indexes from query patterns, transaction boundaries, tenancy, retention, migration strategy, backups/recovery, and privacy classification. Validate with representative queries and scale. Do not use application validation as a substitute for database invariants when the database can enforce them safely.
