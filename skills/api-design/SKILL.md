# API Design

Start from consumers and domain operations. Define resources/commands, schemas, errors, authentication/authorization, idempotency, pagination/filtering, versioning/compatibility, rate limits, observability, and examples. Prefer consistent boring conventions. Treat public API changes as durable contracts and record breaking-change decisions.
