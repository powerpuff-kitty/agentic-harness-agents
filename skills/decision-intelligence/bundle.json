{
  "format_version": 2,
  "kind": "standalone-skill",
  "name": "decision-intelligence",
  "files": ["SKILL.md", "references/decision-guide.md", "references/graph-review.md", "scripts/review_graph.py"],
  "optional_tools": [
    {
      "name": "typesafe-ai",
      "fallback": "Use only explicitly permitted current-agent guidance, without calling it Jev or inventing probabilities. If the task requires an actual Jev response and no authorised provider is available, report provider-unavailable and abstain. Never install the optional skill or obtain credentials automatically."
    },
    {
      "name": "python3",
      "fallback": "Inspect declared dependencies manually; do not install an interpreter or execute graph content automatically."
    }
  ],
  "shared_references": {},
  "optional_scripts": [
    {
      "path": "scripts/review_graph.py",
      "execution": "explicit-invocation-only",
      "fallback": "Check node identity, prerequisites and reducer inputs manually. A valid graph is not permission to batch or execute decisions."
    }
  ]
}
